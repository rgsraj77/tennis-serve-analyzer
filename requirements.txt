# mediapipe brings opencv-contrib-python with it — do not add opencv-python
# separately or the two wheels will fight over the cv2 package.
# That wheel needs libGL/libglib at runtime, which slim Linux containers lack:
# packages.txt installs them (Streamlit Cloud reads it; on other hosts install
# libgl1 + libglib2.0-0 yourself, or cv2 fails at import).
mediapipe==0.10.14
numpy>=1.26,<2
scipy>=1.11
matplotlib>=3.8
imageio-ffmpeg>=0.4.9

# Streamlit frontend (streamlit_app.py) — the hosted deploy target
streamlit>=1.35

# FastAPI frontend (app.py) — local/self-hosted
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# dev only: measure_memory.py
psutil>=5.9
